{
  "name": "tuft.dev",
  "version": "1.0.0",
  "description": "Offical website of the Tuft Node.js web framework.",
  "private": true,
  "scripts": {
    "purgecss": "purgecss --config ./purgecss.config.js",
    "build": "JEKYLL_ENV=production bundle exec jekyll build && npm run purgecss",
    "start": "bundle exec jekyll serve --livereload"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/tuftjs/tuft.dev.git"
  },
  "devDependencies": {
    "purgecss": "^3.1.0"
  }
}
